server:
  port: ${PORT:8000}
spring:
  application:
    name: synapse-api
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/synapse}
app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4200,http://127.0.0.1:4200}
